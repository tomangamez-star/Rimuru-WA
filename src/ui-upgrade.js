'use strict'
const baseUi=require('./ui'),{createRpg}=require('./rpg'),{createRpgUpgrade}=require('./rpg-upgrade'),{canonicalUserId,displayName,commandParts}=require('./economy-router'),ai=require('./ai-v2')
const META=/\b(ai|model|groq|whatsapp|bot|command|menu|button|quest ?log|bug|glitch|database|supabase|render|code|coding|server|deploy|redeploy|cooldown|how (?:do|can) i (?:use|end|stop)|not working|doesn't work|isn't working)\b/i
function createUi(opts){const base=baseUi.createUi(opts),rpg=createRpg({economy:opts.economy,logger:opts.logger,sendButtons:opts.sendButtons}),up=createRpgUpgrade({base:rpg,economy:opts.economy,sendButtons:opts.sendButtons}),masters=new Set(),send=(s,j,m,t)=>s.sendMessage(j,{text:t},{quoted:m}),P=m=>({id:canonicalUserId(m.key),name:displayName(m),jid:m.key.remoteJid})
 async function route(s,m,content){const c=commandParts(content);if(c){if(await up.route(s,m,c))return true;if(c.command==='/talk'){const r=await base.route(s,m,content);masters.add(P(m).id);return r}if(c.command==='/endtalk'){masters.delete(P(m).id);return base.route(s,m,content)}}return base.route(s,m,content)}
 async function section(s,m,x){if(await up.section(s,m,x))return true;const r=await base.section(s,m,x);if(x==='rpg_talk_master')masters.add(P(m).id);return r}
 async function maybeAi(s,m,content){const p=P(m),text=String(content.conversation||content.extendedTextMessage?.text||'').trim();if(!text||text.startsWith('/')||m.key.fromMe)return false;const npc=await rpg.npcContext(p.id),master=masters.has(p.id);if(!npc&&!master)return base.maybeAi?.(s,m,content)||false;const player=master?await rpg.getPlayer(p.id):null,meta=META.test(text);const mode=meta?'rimuru':master?'master':'npc';const a=await ai.chat({jid:p.jid,userId:p.id,name:p.name,text,mode,player,npc:mode==='npc'?npc:null});if(master&&!meta)await rpg.recordMasterChat(p.id);await send(s,p.jid,m,mode==='npc'?`*${npc.name}:* ${a}`:mode==='master'?`*${player.currentMaster}:* ${a}`:`🌊 *Rimuru:* ${a}`);return true}
 return{...base,route,section,maybeAi}}
module.exports={createUi}
